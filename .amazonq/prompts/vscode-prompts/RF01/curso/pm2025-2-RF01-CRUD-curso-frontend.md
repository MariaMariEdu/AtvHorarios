@frontend

respostas em pt-br

1.) editar o componente src/component/Menu/Menu.jsx
    a) rotear componentes (Router).
        a.1) ao selecionar **Cursos**, exibir o componente **Cursos** em um modal;
        a.2) controles para fechar o modal (X e o botão Fechar)

2.) criar o componente src/component/Cursos/Cursos.jsx
a.) funcionalidades:
    a.1) grid mostrando os **Cursos** com controles para inserir, alterar e excluir (CRUD)
    a.2) ao clicar no cabeçalho da coluna ordenar o conteúdo do grid pelo conteúdo da coluna (Sorting)
    a.3) filtrar conteúdo do grid (Filter/Search)
b) integrar componente com o endpoint **cursos**

3.) Critérios de aceitação
a.) A app deve ser responsiva

4.) Gerar documentação e comentários JSDoc