@projeto @backend

resposta: pt-br

Crie um modelo Mongoose para a collection `professores` e gere a documentação JSDoc para ele. A resposta deve estar formatada em Markdown.

O modelo de professor deve incluir os seguintes campos e tipos:

* **`nome`**: Nome completo do professor (String, Obrigatório).
* **`email`**: E-mail institucional do professor (String, Obrigatório, Único).
* **`telefone`**: Telefone de contato do professor (String, Opcional).
* **`status`**: Status do professor (String ou Boolean, Ex: 'Ativo', 'Inativo', true/false).

Inclua também o código JSDoc para documentar a estrutura do Schema e seus campos.
